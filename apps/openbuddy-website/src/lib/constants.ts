/**
 * Site-wide stats — single source of truth.
 * Update here only; do NOT hardcode these numbers in component files.
 */
export const SITE_STATS = {
  tests: 1886,
  packages: 64,
  specs: 634,
  stars: 8
} as const;

export const SITE_LICENSE = 'MIT' as const;
